spring:
  application:
    name: seed-ai

  datasource:
    url: jdbc:postgresql://localhost:5432/seedAiDB
    username: postgres
    password: root3
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

        format_sql: true

server:
  port: 8081
